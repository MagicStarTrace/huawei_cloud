# 华为云 GPS 同步服务 - 环境变量配置

# ==================== 核心配置 ====================

# Storage state 文件路径（保存登录会话）
# 容器环境推荐: /data/storage_state.json
# 本地开发推荐: ./storage_state.json
STORAGE_STATE_PATH=/data/storage_state.json

# 调试模式 - 1=开启详细日志，0=关闭
DEBUG_MODE=1

# 服务端口
PORT=8080

# ==================== 可选配置 ====================

# 高德地图 API Key（用于逆地理编码获取地址）
# 申请地址: https://console.amap.com/dev/key/app
# 如果不配置，地址字段将返回 null，reason=NO_GAODE_KEY
AMAP_API_KEY=

# 会话超时时间（秒）- 默认3600秒（1小时）
SESSION_EXPIRE_TIME=3600

# ==================== 认证方式 ====================
# 方式1（推荐）：POST /auth/cookies 上传浏览器导出的 cookies
# 方式2：访问 /auth/ensure 手动登录（需要 DISPLAY 环境）
# 方式3：手动将 storage_state.json 放到 STORAGE_STATE_PATH
